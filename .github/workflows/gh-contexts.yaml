name: Github context
on: push
jobs:
  learn-gh-context:
    runs-on: ubuntu-latest
    steps:
      - name: "Step 1"
        run: | 
          echo "Hello! $MY_ACTION"
        env:
          MY_ACTION: ${{ github.action }}